{
  "name": "gtvet-wel-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "build": "npm run build --prefix client",
    "render-build": "npm ci --omit=dev --prefix server && npm ci --include=dev --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
